/** 角色状态枚举 */
export enum FighterState {
  IDLE = "idle",
  WALK = "walk",
  ATTACK = "attack",
  BLOCK = "block",
  HIT = "hit",
}

/** 游戏模式 */
export enum GameMode {
  VS_2P = "vs_2p",
  VS_CPU = "vs_cpu",
}
